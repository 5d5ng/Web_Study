var a="something";
var res = (a)? "ok":"not ok"; //if a is True or has Value

console.log(res);  //print OK  a가 비어있다면 not ok출력

var arr = [1,2,3];
for(var i=0 ,len = arr.length ;i<len;i++)
{
    console.log(arr[i]);
}